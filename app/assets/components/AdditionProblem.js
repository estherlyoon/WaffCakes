import * as React from "react";
import { StyleSheet, StatusBar, Text, View, Button, Image } from "react-native";

export default function AdditionProblem({ navigation }) {
  return <View>4 + 4</View>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    borderColor: "#20232a",
  },
});
